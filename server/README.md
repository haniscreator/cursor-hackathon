Local server for AI Virtual Tour.

.env.local variables:
- OLLAMA_URL (default http://localhost:11434)
- ELEVENLABS_KEY (optional)
- PORT (default 3000)
- USE_MOCK (set to "true" to use mock responses from mock-responses.json)

Run:
npm install
npm start
